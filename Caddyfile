# SSL/HTTPS automático via Let's Encrypt (defina DOMAIN no .env do servidor).
{$DOMAIN} {
	reverse_proxy web:3000
	encode zstd gzip
	header {
		-Server
		Strict-Transport-Security "max-age=63072000; includeSubDomains; preload"
	}
}
